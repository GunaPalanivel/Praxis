# Evidence Package — Numbers + Plots Judges Can Verify

> Anything we cite from the stage must be reproducible from this folder.
> Targets the 20% **"Showing improvement in rewards"** axis plus the 10%
> **"Reward & training pipeline"** axis. Updated for MissionOps + composable rubrics + outcome × efficiency.

---

## 1. The 4-row score gap table (must-have, low-cost)

Saved to `docs/baseline_scores.md`. Row 4 added after Issue #32 training run.

| Agent                                      | Mean score (outcome × efficiency) | Behaviour                                              |
| ------------------------------------------ | --------------------------------- | ------------------------------------------------------ |
| Random baseline                            | ~0.02                             | Random commands; outcome gate fails; score floor.      |
| Qwen2.5-7B (no system prompt)              | ~0.06                             | Some investigation, never plans, fails outcome gate.   |
| Qwen2.5-7B (SRE + MissionOps system prompt) | ~0.18                             | Plans, partial diagnosis, sometimes resolves.          |
| **Qwen2.5-7B (mtGRPO trained on Praxis)** | **~0.31**                         | Plan, save_finding, recall_memory, recover, submit.    |

The **5–8× gap** between rows 1/2 and row 4 is the "training signal exists" proof. Issue #30 owns rows 1–3; Issue #32 appends row 4 after the training run.

Reproduction commands:

```bash
python inference.py --task cascading-platform-failure --runs 5 --seed 2026 --model random
python inference.py --task cascading-platform-failure --runs 5 --seed 2026 --no-system-prompt
python inference.py --task cascading-platform-failure --runs 5 --seed 2026
python inference.py --task cascading-platform-failure --runs 5 --seed 2026 --adapter ./checkpoints/praxis-grpo
```

---

## 2. Reward + loss curves (Issue #32)

Saved to `docs/figures/reward_curve.png` and `docs/figures/loss_curve.png`. Generated by `train_praxis_grpo.py` with Trackio + WandB:

- ≥50 mtGRPO steps (≈45 min on A10G; ≈90 min on Colab T4 with Qwen-3B fallback).
- Model: `Qwen/Qwen2.5-7B-Instruct` (fallback `Qwen2.5-3B-Instruct`).
- Tasks: `cascading-platform-failure` for the headline curve; `single-service-alert` for sanity.
- X axis: training step. Y axis: mean reward (reward curve) / loss (loss curve).
- Two lines: trained vs cached random baseline on same axes.

Public links go in `docs/training_links.md` and the README:

```
Trackio: https://trackio.example/runs/<id>
WandB:   https://wandb.ai/<team>/praxis-mission-ops/runs/<id>
```

---

## 3. Before-vs-after rollout (Issue #33 — the trophy moment)

| File                       | What's in it                                                                          |
| -------------------------- | ------------------------------------------------------------------------------------- |
| `docs/rollout_baseline.txt` | Full text trajectory of baseline Qwen-7B; final score 0.04.                           |
| `docs/rollout_trained.txt` | Same seed/mission after Issue #32 training; final score 0.31.                         |
| `docs/figures/rollout_compare.png` | Per-turn reward chart, both lines on same axes.                                       |
| `docs/demo.gif`            | 8-second loop of the cutoff moment (baseline panics, trained calls `recall_memory`).  |

This is what opens the README, the slide deck, and the live demo.

---

## 4. Per-rubric attribution chart (story booster)

Optional, ~30 min after Issue #32. Bar chart broken down per rubric:

- `PlanningRubric` contribution
- `MemoryRubric` contribution
- `RecoveryRubric` contribution
- `TerminalRubric` contribution

Saved to `docs/figures/rubric_attribution.png`. Demonstrates the moat in numbers — judges checklist item #13 ("composable rubrics > monolithic scoring").

---

## 5. Determinism receipt

Required by judging Phase 1 ("Graders that always return the same score" → DQ). Save the result of:

```bash
python -m pytest tests/test_scenarios.py tests/test_artifacts.py tests/test_rubrics.py -q -k determinism
```

…to `docs/determinism_receipt.txt`. Re-run 3× to prove no randomness in scenarios, ArtifactStore draws, or rubric scoring.

---

## 6. Runtime + resource receipts

```
docs/runtime_receipt.txt
  python inference.py --task <each>
  → completes in 4–12 min per task on vCPU=2 / 8 GB
docs/resource_receipt.txt
  Peak RSS during 6-task run: < 1.6 GB
```

These satisfy the infra restriction in `idea/PROBLEM STATEMENT/How Judging works.md` ("Runtime < 20 min", "vCPU 2 / mem 8 GB").

---

## 7. Citations to drop into the README

- Rootly AI Labs `logs-dataset` (Apache-2.0) — real production logs (S33).
- arxiv AgeMem — _"memory operations as tool-based actions, GRPO-compatible"_ (S28).
- arxiv 2601.07190 — _"Context Bloat… passive summarization fails"_ (S29).
- GRPO survey — _"process rewards are more informative than terminal rewards"_ (S30).
- Unsloth + mtGRPO — turn-level credit assignment, ~2.5× throughput on multi-turn agents (S35).
- UltraHorizon — long-horizon failure-mode taxonomy validating the MissionOps gap (S36).

Format for the README:

> Praxis MissionOps's planning + memory + recovery layer is grounded in arxiv AgeMem (memory ops as GRPO-compatible tool actions), addresses Context Bloat (arxiv 2601.07190), and follows the GRPO survey insight that process rewards beat terminal rewards. Real Rootly production logs (Apache-2.0) feed the artifacts. Training uses Unsloth + multi-turn GRPO for turn-level credit assignment.

---

## 8. The submission package inventory (owned by Issue #38)

- `README.md` (rewritten — Issue #38) opening with `docs/figures/rollout_compare.png` + `docs/demo.gif`
- `docs/baseline_scores.md` (4 rows; Issues #30 + #32)
- `docs/figures/reward_curve.png` (Issue #32)
- `docs/figures/loss_curve.png` (Issue #32)
- `docs/figures/rubric_attribution.png` (optional)
- `docs/rollout_baseline.txt` (Issue #33)
- `docs/rollout_trained.txt` (Issue #33)
- `docs/figures/rollout_compare.png` (Issue #33)
- `docs/demo.gif` (Issue #33)
- `docs/determinism_receipt.txt`
- `docs/runtime_receipt.txt`
- `docs/training_links.md` (Trackio + WandB public URLs)
- HF Space URL (Issue #37)
- Mini-blog URL **or** YouTube link (≤2 min) (Issue #38)
- Slide deck URL (Google Slides or PDF) (Issue #38)
- Trackio + WandB public run URLs

All linked from the top of `README.md` so judges find them in 30 seconds.
