# server/requirements.txt — Production dependencies for the Docker container

# OpenEnv framework
openenv-core

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP client (for the environment client — not server-side, but needed for full package)
httpx>=0.27.0

# OpenAI client (for inference.py — installed at build time so image is self-contained)
openai>=1.30.0
